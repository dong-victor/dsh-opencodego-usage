# dependencies (installed locally for dev/test; the published package ships
# no node_modules — @deepseek-ai/dsh-tools is a peer dep resolved from the host DSH)
node_modules/

# npm pack artifacts
*.tgz

# logs
*.log
